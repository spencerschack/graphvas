import { isSafe } from '../../../helpers/is-safe';
import { module, test } from 'qunit';

module('Unit | Helper | is safe');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isSafe(42);
  assert.ok(result);
});
